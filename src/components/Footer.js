import React from 'react';

const Footer = () => {
    return (
        <footer className="sticky bottom-0 w-full h-12 bg-sky-500 flex items-center justify-center transition-all duration-300 hover:bg-sky-700">
            <div className="text-white text-xl p-4 text-center">
                Echo Atrium Chat
            </div>
        </footer>
    );
};

export default Footer;
