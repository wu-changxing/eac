import React from 'react';

const Footer = () => {
    return (
        <footer className="fixed bottom-0 w-full h-12 bg-sky-600">
            <div className="text-gray-200">
                Echo Atrium Chat
            </div>
        </footer>
    );
};

export default Footer;
